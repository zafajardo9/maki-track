name: Publish MCP Package

on:
  release:
    types: [published]
  push:
    branches: [main]
    paths:
      - packages/mcp/package.json
      - packages/mcp/server.json

permissions:
  contents: write
  id-token: write

concurrency:
  group: publish-mcp
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    if: ${{ github.event_name == 'push' || startsWith(github.event.release.tag_name, 'mcp-v') }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.32.1

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24.19.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run MCP checks
        run: |
          pnpm --filter @maki/mcp test
          pnpm --filter @maki/mcp build

      - name: Validate MCP release tag
        if: ${{ github.event_name == 'release' }}
        run: |
          PACKAGE_VERSION=$(node -p "require('./packages/mcp/package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#mcp-v}"

          if [ "${GITHUB_REF_NAME}" = "${TAG_VERSION}" ]; then
            echo "Release tag must start with mcp-v, got ${GITHUB_REF_NAME}"
            exit 1
          fi

          if [ "${TAG_VERSION}" != "${PACKAGE_VERSION}" ]; then
            echo "Release tag version (${TAG_VERSION}) does not match packages/mcp version (${PACKAGE_VERSION})"
            exit 1
          fi

      - name: Check whether @maki/mcp version is already published
        id: version_check
        run: |
          PACKAGE_VERSION=$(node -p "require('./packages/mcp/package.json').version")

          if npm view "@maki/mcp@${PACKAGE_VERSION}" version >/dev/null 2>&1; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            echo "@maki/mcp@${PACKAGE_VERSION} is already published; skipping publish."
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish @maki/mcp
        if: ${{ steps.version_check.outputs.already_published == 'false' }}
        run: |
          cd packages/mcp
          npm publish --access public --provenance

      - name: Publish to the MCP Registry
        run: |
          PACKAGE_VERSION=$(node -p "require('./packages/mcp/package.json').version")
          IN_REGISTRY=$(curl -s "https://registry.modelcontextprotocol.io/v0/servers?search=io.github.usekaneo/kaneo" \
            | jq -r --arg v "$PACKAGE_VERSION" '[.servers[]? | (.server // .) | select(.name == "io.github.usekaneo/kaneo" and .version == $v)] | length')
          if [ "$IN_REGISTRY" != "0" ]; then
            echo "io.github.usekaneo/kaneo@${PACKAGE_VERSION} is already in the MCP Registry; skipping."
            exit 0
          fi
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher
          cd packages/mcp
          jq --arg v "$PACKAGE_VERSION" '.version = $v | .packages[0].version = $v' server.json > server.tmp && mv server.tmp server.json
          ../../mcp-publisher login github-oidc
          ../../mcp-publisher publish

      - name: Create MCP release
        if: ${{ github.event_name == 'push' }}
        run: |
          PACKAGE_VERSION=$(node -p "require('./packages/mcp/package.json').version")

          if gh release view "mcp-v${PACKAGE_VERSION}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            echo "Release mcp-v${PACKAGE_VERSION} already exists; skipping."
            exit 0
          fi

          gh release create "mcp-v${PACKAGE_VERSION}" \
            --target "${GITHUB_SHA}" \
            --title "@maki/mcp v${PACKAGE_VERSION}" \
            --notes "Published [@maki/mcp@${PACKAGE_VERSION}](https://www.npmjs.com/package/@maki/mcp/v/${PACKAGE_VERSION}) to npm." \
            --latest=false
        env:
          GH_TOKEN: ${{ github.token }}
